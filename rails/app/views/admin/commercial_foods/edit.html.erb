<% content_for :title, "Editar #{@commercial_food.name}" %>

<div class="max-w-3xl mx-auto px-4 py-8">
  <div class="mb-6">
    <%= link_to "← Alimentos comerciales", admin_commercial_foods_path,
          class: "text-sm text-primary font-medium hover:underline" %>
  </div>
  <h1 class="text-2xl font-bold text-on-background mb-6">Editar <span class="text-primary"><%= @commercial_food.name %></span></h1>
  <%= render "form", food: @commercial_food %>
</div>
