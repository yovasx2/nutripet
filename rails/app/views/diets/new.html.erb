<% content_for :title, "Nuevo Plan — #{@pet.name}" %>

<div class="max-w-2xl mx-auto px-4 py-10">
  <%= link_to "← #{@pet.name}", @pet, class: "text-sm text-primary font-medium hover:underline block mb-6" %>

  <!-- Pet summary -->
  <div class="bg-white rounded-2xl border border-outline-variant shadow-sm overflow-hidden mb-6">
    <div class="h-2 bg-primary"></div>
    <div class="p-5 flex items-center gap-4">
      <div class="flex-1">
        <p class="font-bold text-on-background text-lg"><%= @pet.name %></p>
        <p class="text-on-surface-variant text-sm">
          <%= @pet.species_label %> · <%= @pet.weight_kg %> kg ·
          <%= @pet.life_stage_label %> · <%= @pet.activity_label %>
          <% if @pet.is_neutered %> · Esterilizado/a<% end %>
        </p>
      </div>
    </div>
  </div>

  <!-- Prep style form -->
  <div class="bg-white rounded-2xl border border-outline-variant shadow-sm p-6">
    <h1 class="text-xl font-bold text-on-background mb-1">Generar plan nutricional</h1>
    <p class="text-on-surface-variant text-sm mb-6">Selecciona el modo del plan para generar el set completo de recetas.</p>

    <%= form_with url: pet_diets_path(@pet), method: :post, class: "space-y-4" do |f| %>
      <div>
        <label class="block text-xs font-semibold text-on-surface-variant mb-1">Modo del plan</label>
        <select name="diet_mode" class="w-full text-sm border border-outline-variant rounded-lg px-3 py-2 bg-white focus:outline-none focus:ring-2 focus:ring-primary">
          <option value="cooked" selected>Cocida</option>
          <option value="mixed">Mixto</option>
          <option value="raw"><%= Diet::PREP_STYLES["raw"] %></option>
        </select>
      </div>

      <button type="submit"
        class="w-full bg-primary text-white text-sm font-semibold py-3 rounded-xl hover:bg-primary-container transition-colors mt-2">
        Generar plan
      </button>
    <% end %>
  </div>
</div>
